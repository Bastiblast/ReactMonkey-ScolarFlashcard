import React from "react";

interface StatistiquesButtonProps {
  carouselButtonName: string;
  children?: React.ReactNode;
}

export default function Statistiques(props: StatistiquesButtonProps) {
  return <div>Statistiques</div>;
}
